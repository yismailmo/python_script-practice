# define a class of Person with name and talk attributes

class Person:
    def __init__(self, name):
        self.name = name

    def talk(self):
        print(f"Hi I am {self.name}")


yaska = Person('yaska')
yaska.talk()
xamdi = Person("Xamdi")
xamdi.talk()
